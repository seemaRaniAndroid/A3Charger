<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.parangat.a3charger">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.A3Charger"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".UI.Activities.Detail.ReviewDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Activities.Detail.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Activities.Venue.VenueActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Activities.Login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>